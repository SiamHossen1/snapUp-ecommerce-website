import { TopNavbar, MainNavbar } from "../pages";

const Header = () => {
  return (
    <header className=" bg-orange-500 px-20 text-white">
      <TopNavbar />
      <MainNavbar />
    </header>
  );
};

export default Header;
